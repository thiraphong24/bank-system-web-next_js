// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface TransferReceiveModel {
    user: string
    name: string
    remain: number
    action: string
    fromOrTo: string
    amount: number
    transferDate: string
}